#include <stdio.h>

int main(void)
{

    // in c the programmers follow the convetion of using only lower-case letters in identifiers
    //  symbol_table current_page

    // there is limit on the name of the identifier
    // C89 first 31 characters
    // C99 63 characters

    return 0;
}